services:
  - type: web
    name: stockron-ai
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn ai_analyzer_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
